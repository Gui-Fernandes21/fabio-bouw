{
  "name": "fabio-bouw",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "build:deploy": "nuxt build --preset=firebase && npx firebase-tools deploy"
  },
  "dependencies": {
    "axios": "^1.7.9",
    "firebase-functions": "^6.1.0",
    "firebase-tools": "^13.22.1",
    "nuxt": "^3.13.0",
    "vue": "latest",
    "vue-router": "latest"
  }
}
